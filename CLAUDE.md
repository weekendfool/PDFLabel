# PDFラベルメーカー

## プロジェクト概要
テキストを入力してラベル用紙に合わせたPDFを生成するWebアプリケーション

## 技術仕様
- HTML + JavaScript (サーバー不要、ブラウザだけで動く)
- PDF生成には jsPDFライブラリを使う (CDNから読み込む)

## ラベルの仕様
- 用紙サイズ: A4(210mm × 297mm)
- 余白(上下左右)を設定できる
- ラベルのサイズ(幅 × 高さ)を設定できる
- ラベル間の余白を設定できる
- テキストを入力し、すべてのラベルに同じ内容を印字する
- ラベルの配置(列数×行数)は、用紙サイズ・余白・ラベルサイズ・ラベル間余白から自動計算する
- 日本語テキストの印字に対応する(日本語TTFフォントをBase64化してjsPDFに埋め込む)
- テキストがラベル枠に収まらない場合は、フォントサイズを自動縮小して枠内に収める
- 印字位置の微調整用に、上下左右オフセット(mm)を入力できる(プリンタの個体差によるズレを補正するため)
- PDF生成前に、HTML上でラベル配置のレイアウトプレビューをリアルタイム表示する
- ラベル境界のガイド線(細線)をPDFに印字するかどうかをON/OFFで切り替えられる
- テキスト入力欄はtextareaとし、改行を含む複数行入力を許容してそのまま複数行印字する
- 用紙・余白・ラベルサイズなどの入力設定はlocalStorageに保存し、次回起動時に復元する

## コーディング規約
 - コメントは日本語で書く
 - 一つの HTMLファイルにまとめる (外部CSSや外部JSファイルは作成しない)

 ## 作業ルール
 - 変更前に必ず git commitすること (hookファイルに設定)